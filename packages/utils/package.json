{
  "name": "@exotic-nursery/utils",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint config yet\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@exotic-nursery/types": "workspace:*",
    "zod": "^3.25.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
