{
  "name": "@exotic-nursery/supabase",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint config yet\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "@exotic-nursery/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
